import React from "react";
import { Link, useHistory } from "react-router-dom";

const Activities = ({ userData, setUserData, setToken }) => {
return(
<>
    <h3> this is the Activities page</h3>
</>)
};
export default Activities;
import React from "react";
import { Link, useHistory } from "react-router-dom";

const Routines = ({ userData, setUserData, setToken }) => {
return(
<>
    <h3> this is the Routines page</h3>
</>)
};
export default Routines;